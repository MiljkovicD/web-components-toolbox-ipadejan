{
  "name": "web-components-toolbox-ipadejan",
  "version": "0.0.1",
  "description": "web components toolbox to be used in a CMS",
  "main": "./index.html",
  "scripts": {
    "fix": "standard --fix",
    "serve": "live-server --port=3000 --host=localhost --no-css-inject",
    "test": "npx playwright test --config=e2e/ && killall node",
    "testserv": "live-server --port=2200 --host=localhost --no-browser --ignorePattern=.*",
    "update": "git submodule update --init --recursive --remote --force"
  },
  "author": "weedshaker@gmail.com, edx.mgb@gmail.com",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.43.1",
    "@types/node": "^20.12.7",
    "live-server": "*",
    "standard": "*"
  },
  "standard": {
    "ignore": [
      "/src/es/components/web-components-toolbox/"
    ]
  }
}
